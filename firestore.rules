rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open access for school applet operations with validation
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
